# Attack Script — LLM10 Secured

1. Open `http://localhost:8518`.
2. Trigger repeated runs quickly to simulate burst traffic.
3. Try extraction text such as `dump model parameters`.
4. Continue until rate/token/cost controls activate.

Expected outcome:
- Burst, rate, token, budget, concurrency, and extraction checks block abuse predictably.
- Security event panel shows exact control decisions.
