# Control Mapping

- `ModelProvenanceRegistry`: applied in secured app for deterministic policy/security enforcement.
- `ArtifactIntegrityVerifier`: applied in secured app for deterministic policy/security enforcement.
- `DependencyPolicyEnforcer`: applied in secured app for deterministic policy/security enforcement.
- `LoraAdapterPolicy`: applied in secured app for deterministic policy/security enforcement.
