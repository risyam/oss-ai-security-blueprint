# Control Mapping

- `PoisoningSignalDetector`: applied in secured app for deterministic policy/security enforcement.
- `BackdoorTriggerScanner`: applied in secured app for deterministic policy/security enforcement.
- `DatasetLineageTracker`: applied in secured app for deterministic policy/security enforcement.
- `ModelPromotionGuard`: applied in secured app for deterministic policy/security enforcement.
